name: Auth Pipeline

on:
  push:
    paths:
      - "app/auth-service/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Image
        run: |
          docker build \
          -t auth-service \
          ./app/auth-service
